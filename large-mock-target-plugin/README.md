# large-mock-target-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build large-mock-target-plugin` to build the library.

## Running unit tests

Run `nx test large-mock-target-plugin` to execute the unit tests via [Jest](https://jestjs.io).
